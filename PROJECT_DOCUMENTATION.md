# Документация проекта NUR CRM Frontend

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Технологический стек](#технологический-стек)
3. [Архитектура приложения](#архитектура-приложения)
4. [Структура проекта](#структура-проекта)
5. [Функциональность по секторам](#функциональность-по-секторам)
6. [API интеграция](#api-интеграция)
7. [Управление состоянием](#управление-состоянием)
8. [Роутинг](#роутинг)
9. [Интернационализация](#интернационализация)
10. [Установка и запуск](#установка-и-запуск)
11. [Сборка для production](#сборка-для-production)
12. [Разработка](#разработка)
13. [Безопасность](#безопасность)
14. [Производительность](#производительность)

---

## 🎯 Обзор проекта

**NUR CRM** — это комплексная CRM-система для управления бизнесом в различных отраслях. Платформа предоставляет единый интерфейс для управления кафе, строительными проектами, маркетами, барбершопами/салонами красоты, школами, гостиницами, консалтинговыми агентствами, производством и складами.

### Основные возможности

- ✅ Многопользовательская система с ролевым доступом
- ✅ Управление несколькими филиалами и отделами
- ✅ Интеграция с социальными сетями (WhatsApp, Telegram, Instagram)
- ✅ Аналитика и отчетность
- ✅ Управление складом и товарами
- ✅ Кассовые операции
- ✅ Управление клиентами и сотрудниками
- ✅ Мобильная адаптивность
- ✅ Многоязычность (русский, кыргызский)

### Поддерживаемые секторы

1. **Кафе** — столы, заказы, кухня/KDS, меню, резервации
2. **Строй-сфера** — сметы, заявки, объекты, материалы, акты
3. **Маркет** — товары, остатки, штрих-коды, касса, скидки
4. **Барбершоп/Салон** — записи, мастера, услуги, клиентская база
5. **Школа** — лиды, группы, расписания, счета, рассрочки
6. **Гостиница** — номера, брони, заселение/выезд, отчёты
7. **Консалтинг** — клиенты, заявки, услуги, бронирования
8. **Производство** — каталог, заявки, агенты, аналитика
9. **Склад** — товары, движения, поставки, списания

---

## 🛠 Технологический стек

### Основные технологии

- **React 18.2.0** — UI библиотека
- **React Router DOM 6.22.3** — маршрутизация
- **Redux Toolkit 2.2.3** — управление состоянием
- **Axios 1.6.8** — HTTP клиент
- **SASS 1.69.7** — препроцессор CSS
- **Tailwind CSS 3.4.18** — utility-first CSS фреймворк

### UI библиотеки

- **Material-UI (MUI) 5.18.0** — компоненты интерфейса
- **Radix UI** — доступные компоненты (Dialog, Select, Tabs, Checkbox)
- **Lucide React** — иконки
- **React Icons** — дополнительные иконки

### Графики и визуализация

- **Chart.js 4.4.3** — графики
- **React Chart.js 2** — React обертка для Chart.js
- **Recharts 3.1.0** — библиотека графиков

### Дополнительные библиотеки

- **i18next 23.16.8** — интернационализация
- **date-fns 2.30.0** — работа с датами
- **FullCalendar 6.1.18** — календарь
- **html5-qrcode 2.3.8** — сканирование QR-кодов
- **jsbarcode 3.12.1** — генерация штрих-кодов
- **Sonner 2.0.7** — уведомления (toast)

### Инструменты разработки

- **Create React App** — сборщик проекта
- **PostCSS** — обработка CSS
- **Autoprefixer** — автопрефиксы для CSS

---

## 🏗 Архитектура приложения

### Общая архитектура

```
┌─────────────────────────────────────────┐
│           React Application             │
├─────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐             │
│  │  Routes  │  │  Layout  │             │
│  └──────────┘  └──────────┘             │
│  ┌──────────────────────────────────┐   │
│  │      Redux Store (RTK)           │   │
│  │  ┌────────┐  ┌────────┐          │   │
│  │  │Slices  │  │Creators│          │   │
│  │  └────────┘  └────────┘          │   │
│  └──────────────────────────────────┘   │
│  ┌──────────────────────────────────┐   │
│  │      API Layer (Axios)           │   │
│  │  ┌────────┐  ┌────────┐          │   │
│  │  │Services│  │Interceptors│      │   │
│  │  └────────┘  └────────┘          │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### Паттерны проектирования

1. **Container/Presentational** — разделение логики и представления
2. **Custom Hooks** — переиспользуемая бизнес-логика
3. **Redux Toolkit** — централизованное управление состоянием
4. **API Services** — абстракция над HTTP запросами
5. **Protected Routes** — защита маршрутов

---

## 📁 Структура проекта

```
frontend-actual/
├── public/                 # Статические файлы
│   ├── index.html
│   ├── favicon.png
│   └── manifest.json
│
├── src/
│   ├── api/               # API сервисы
│   │   ├── index.js      # Axios конфигурация
│   │   ├── auth.js       # Аутентификация
│   │   ├── orders.js     # Заказы
│   │   ├── products.js   # Товары
│   │   └── ...
│   │
│   ├── Components/       # React компоненты
│   │   ├── Auth/         # Авторизация
│   │   ├── common/       # Общие компоненты
│   │   ├── Contact/      # Контакты
│   │   ├── Debts/        # Долги
│   │   ├── Department/   # Отделы
│   │   ├── Deposits/     # Депозиты/Склады
│   │   ├── Header/       # Шапка
│   │   ├── Layout/       # Основной layout
│   │   ├── pages/        # Страницы
│   │   ├── Sectors/      # Секторы бизнеса
│   │   │   ├── Barber/   # Барбершоп
│   │   │   ├── cafe/     # Кафе
│   │   │   ├── Market/   # Маркет
│   │   │   ├── School/   # Школа
│   │   │   ├── Hostel/   # Гостиница
│   │   │   ├── Building/ # Стройка
│   │   │   ├── Consulting/ # Консалтинг
│   │   │   ├── Production/ # Производство
│   │   │   └── Warehouse/  # Склад
│   │   └── Sidebar/      # Боковое меню
│   │
│   ├── hooks/            # Custom hooks
│   │   ├── useDebounce.js
│   │   ├── useTransfers.js
│   │   └── ScrollToTop.jsx
│   │
│   ├── locales/          # Переводы
│   │   ├── i18n.js
│   │   ├── ru.json
│   │   └── kg.json
│   │
│   ├── store/            # Redux store
│   │   ├── index.js      # Конфигурация store
│   │   ├── slices/       # Redux slices
│   │   │   ├── userSlice.js
│   │   │   ├── orderSlice.js
│   │   │   ├── productSlice.js
│   │   │   └── ...
│   │   └── creators/     # Async thunks
│   │       ├── userCreators.js
│   │       ├── orderCreators.js
│   │       └── ...
│   │
│   ├── App.js            # Главный компонент
│   ├── App.scss          # Глобальные стили
│   ├── index.js          # Точка входа
│   ├── index.css         # Базовые стили
│   ├── i18n.js           # Конфигурация i18n
│   └── ProtectedRoute.js # Защита маршрутов
│
├── build/                # Собранное приложение
├── package.json          # Зависимости
├── tailwind.config.js    # Конфигурация Tailwind
├── postcss.config.js     # Конфигурация PostCSS
└── README.md             # Базовая документация
```

---

## 🎨 Функциональность по секторам

### 1. Кафе (`/cafe/*`)

**Маршруты:**

- `/cafe/analytics` — аналитика
- `/cafe/menu` — управление меню
- `/cafe/orders` — заказы
- `/cafe/tables` — управление столами
- `/cafe/reservations` — резервации
- `/cafe/kassa` — касса
- `/cafe/stock` — склад
- `/cafe/cook` — кухня/KDS
- `/cafe/inventory` — инвентаризация
- `/cafe/staff` — персонал
- `/cafe/payroll` — зарплата
- `/cafe/purchasing` — закупки
- `/cafe/reports` — отчеты
- `/cafe/clients` — клиенты
- `/cafe/documents` — документы

**Основные возможности:**

- Управление столами и зонами
- Создание и обработка заказов
- Интеграция с кухонным дисплеем (KDS)
- Управление меню и категориями
- Резервации столов
- Кассовые операции
- Учет инвентаря

### 2. Барбершоп/Салон (`/barber/*`)

**Маршруты:**

- `/barber/services` — услуги
- `/barber/masters` — мастера
- `/barber/records` — записи
- `/barber/clients` — клиенты
- `/barber/history` — история
- `/barber/cash-reports` — кассовые отчеты
- `/barber/warehouse` — склад
- `/documents` — документы

**Основные возможности:**

- Управление услугами и прайсами
- Расписание мастеров
- Запись клиентов
- История посещений
- Учет расходных материалов

### 3. Маркет (`/market/*`)

**Маршруты:**

- `/market/warehouse` — склад
- `/market/categories` — категории товаров
- `/market/analytics` — аналитика
- `/market/history` — история продаж
- `/market/documents` — документы
- `/market/bar` — бар
- `/clients` — клиенты (общий)

**Основные возможности:**

- Управление товарами и категориями
- Штрих-коды
- Остатки на складе
- Продажи и касса
- Скидки и акции
- История операций

### 4. Школа (`/school/*`)

**Маршруты:**

- `/school/students` — студенты
- `/school/groups` — группы и курсы
- `/school/lessons` — уроки и аудитории
- `/school/teachers` — преподаватели
- `/school/leads` — лиды
- `/school/invoices` — счета
- `/school/documents` — документы

**Основные возможности:**

- Управление студентами и группами
- Расписание занятий
- Учет преподавателей
- Лиды и конверсия
- Выставление счетов
- Рассрочки и оплаты

### 5. Гостиница (`/hostel/*`)

**Маршруты:**

- `/hostel/rooms` — номера и залы
- `/hostel/bookings` — бронирования
- `/hostel/clients` — клиенты
- `/hostel/bar` — бар
- `/hostel/warehouse` — склад
- `/hostel/analytics` — аналитика
- `/hostel/kassa` — касса
- `/hostel/documents` — документы

**Основные возможности:**

- Управление номерами и типами
- Бронирования и заселения
- Учет клиентов
- Операции с баром
- Аналитика загрузки

### 6. Строй-сфера (`/building/*`)

**Маршруты:**

- `/building/objects` — объекты
- `/building/work` — работы

**Основные возможности:**

- Управление объектами
- Сметы и заявки
- Учет материалов
- Акты выполненных работ

### 7. Консалтинг (`/consulting/*`)

**Маршруты:**

- `/consulting/client` — клиенты
- `/consulting/client-requests` — заявки клиентов
- `/consulting/kassa` — касса
- `/consulting/teachers` — преподаватели
- `/consulting/services` — услуги
- `/consulting/sale` — продажи
- `/consulting/salary` — зарплата
- `/consulting/bookings` — бронирования
- `/consulting/analytics` — аналитика

**Основные возможности:**

- Управление клиентами и заявками
- Услуги и продажи
- Бронирования
- Аналитика

### 8. Производство (`/production/*`)

**Маршруты:**

- `/production/warehouse` — склад
- `/production/catalog` — каталог товаров
- `/production/request` — заявки
- `/production/agents` — агенты
- `/production/agents/:agentId/analytics` — аналитика агента

**Основные возможности:**

- Каталог продукции
- Заявки на производство
- Управление агентами
- Аналитика по агентам
- Склад готовой продукции

### 9. Склад (`/warehouse/*`)

**Маршруты:**

- `/warehouse/products` — товары
- `/warehouse/stocks` — остатки
- `/warehouse/movements` — движения
- `/warehouse/supply` — поставки
- `/warehouse/write_offs` — списания
- `/warehouse/directories` — справочники
- `/warehouse/clients` — клиенты
- `/warehouse/analytics` — аналитика

**Основные возможности:**

- Управление товарами
- Учет остатков
- Движения товаров
- Поставки и списания
- Справочники

### Общие разделы

- `/crm/obzor` — обзор/дашборд
- `/crm/analytics` — общая аналитика
- `/crm/sell` — продажи
- `/crm/sklad` — общий склад
- `/crm/kassa` — касса
- `/crm/employ` — сотрудники
- `/crm/departments` — отделы
- `/crm/branch` — филиалы
- `/crm/clients` — клиенты
- `/crm/contact` — контакты
- `/crm/debts` — долги
- `/crm/pending` — ожидающие
- `/crm/logistics` — логистика
- `/crm/logistics-analytics` — аналитика логистики

---

## 🔌 API интеграция

### Конфигурация API

API клиент настроен в `src/api/index.js`:

```javascript
const api = axios.create({
  baseURL: process.env.REACT_APP_API_BASE_URL || "https://app.nurcrm.kg/api",
  timeout: 20000,
  headers: {
    "Content-Type": "application/json",
    Accept: "application/json",
  },
});
```

### Автоматическое обновление токенов

Система автоматически обновляет access token при истечении:

```javascript
api.interceptors.response.use(
  (res) => res,
  async (err) => {
    // Автоматическое обновление токена при 401
    if (err.response?.status === 401 && !originalRequest._retry) {
      // Обновление токена через refresh token
    }
  }
);
```

### Основные API модули

- `auth.js` — аутентификация и авторизация
- `user.js` — пользователи и профили
- `orders.js` — заказы
- `products.js` — товары
- `clients.js` — клиенты
- `employees.js` — сотрудники
- `departments.js` — отделы
- `analytics.js` — аналитика
- `catalog.js` — каталог
- `transfers.js` — переводы
- `notification.js` — уведомления
- `event.js` — события
- `additionalServices.js` — дополнительные услуги

### Пример использования API

```javascript
import api from "../api";
import { getOrders } from "../api/orders";

// В компоненте
const fetchOrders = async () => {
  try {
    const response = await getOrders();
    setOrders(response.data);
  } catch (error) {
    console.error("Ошибка загрузки заказов:", error);
  }
};
```

---

## 🗄 Управление состоянием

### Redux Store структура

Store настроен с использованием Redux Toolkit:

```javascript
const store = configureStore({
  reducer: {
    user: userReducer,
    order: orderReducer,
    product: productsReducer,
    // ... другие редюсеры
  },
});
```

### Redux Slices

Все слайсы находятся в `src/store/slices/`:

- `userSlice.js` — пользователь и компания
- `orderSlice.js` — заказы
- `productSlice.js` — товары
- `employeeSlice.js` — сотрудники
- `analyticsSlice.js` — аналитика
- `logisticsSlice.js` — логистика
- `departmentSlice.js` — отделы
- `clientSlice.js` — клиенты
- `saleSlice.js` — продажи
- `cashSlice.js` — касса
- `transferSlice.js` — переводы
- `acceptanceSlice.js` — приемка
- `returnSlice.js` — возвраты
- `agentSlice.js` — агенты
- `catalogSlice.js` — каталог
- `cartSlice.js` — корзина
- `cafeOrdersSlice.js` — заказы кафе
- `branchSlice.js` — филиалы
- `uiSlice.js` — UI состояние
- `notificationSlice.js` — уведомления
- `eventsSlice.js` — события
- `jobsSlice.js` — задачи
- `consultingSlice.js` — консалтинг
- `instagramSlice.js` — Instagram
- `warehouseAccountingSlice.js` — складской учет

### Async Thunks (Creators)

Асинхронные действия находятся в `src/store/creators/`:

- `userCreators.js` — действия пользователя
- `orderCreators.js` — действия с заказами
- `productCreators.js` — действия с товарами
- `transferCreators.js` — действия с переводами
- И другие...

### Пример использования

```javascript
import { useDispatch, useSelector } from "react-redux";
import { getOrders } from "../store/creators/orderCreators";
import { useUser } from "../store/slices/userSlice";

function OrdersComponent() {
  const dispatch = useDispatch();
  const orders = useSelector((state) => state.order.orders);
  const { company } = useUser();

  useEffect(() => {
    dispatch(getOrders());
  }, [dispatch]);

  return <div>{/* Отображение заказов */}</div>;
}
```

---

## 🛣 Роутинг

### Структура маршрутов

Приложение использует React Router v6 с вложенными маршрутами:

```javascript
<BrowserRouter>
  <Routes>
    {/* Публичные маршруты */}
    <Route path="/" element={<Landing />} />
    <Route path="/login" element={<Login />} />
    <Route path="/register" element={<Register />} />

    {/* Защищенные маршруты */}
    <Route path="/crm" element={<Layout />}>
      <Route
        path="analytics"
        element={
          <ProtectedRoute>
            <Analytics />
          </ProtectedRoute>
        }
      />
      {/* ... другие маршруты */}
    </Route>
  </Routes>
</BrowserRouter>
```

### ProtectedRoute

Все защищенные маршруты обернуты в `ProtectedRoute`, который:

1. Проверяет наличие компании
2. Проверяет срок действия подписки
3. Перенаправляет на главную при отсутствии доступа

```javascript
const ProtectedRoute = ({ children }) => {
  const { company, companyLoading } = useUser();

  if (companyLoading) return <div>Загрузка...</div>;
  if (!company?.end_date) return <Navigate to="/" replace />;

  // Проверка срока подписки
  const endDate = new Date(company.end_date);
  const now = new Date();
  if (endDate < now) return <Navigate to="/" replace />;

  return children;
};
```

### Динамические маршруты

- `/crm/sell/:id` — детали продажи
- `/crm/departments/:id` — детали отдела
- `/crm/branch/:id` — детали филиала
- `/crm/clients/:id` — детали клиента
- `/crm/kassa/:id` — детали кассы
- `/production/agents/:agentId/analytics` — аналитика агента

---

## 🌍 Интернационализация

### Настройка i18n

Интернационализация настроена через i18next:

```javascript
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import HttpBackend from "i18next-http-backend";
import LanguageDetector from "i18next-browser-languagedetector";

i18n
  .use(HttpBackend)
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    fallbackLng: "ru",
    debug: true,
    interpolation: { escapeValue: false },
  });
```

### Поддерживаемые языки

- **Русский (ru)** — основной язык
- **Кыргызский (kg)** — дополнительный язык

### Файлы переводов

- `src/locales/ru.json` — русские переводы
- `src/locales/kg.json` — кыргызские переводы

### Использование в компонентах

```javascript
import { useTranslation } from "react-i18next";

function MyComponent() {
  const { t } = useTranslation();

  return <h1>{t("welcome.title")}</h1>;
}
```

### Компонент переключения языка

Компонент `Lang` (`src/Components/Lang/Lang.jsx`) позволяет переключать язык интерфейса.

---

## 🚀 Установка и запуск

### Требования

- **Node.js** >= 14.x
- **npm** >= 6.x или **yarn** >= 1.x

### Установка зависимостей

```bash
npm install
# или
yarn install
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
REACT_APP_API_BASE_URL=https://app.nurcrm.kg/api
```

### Запуск в режиме разработки

```bash
npm start
# или
yarn start
```

Приложение откроется по адресу [http://localhost:3000](http://localhost:3000)

### Запуск тестов

```bash
npm test
# или
yarn test
```

---

## 📦 Сборка для production

### Создание production сборки

```bash
npm run build
# или
yarn build
```

Собранные файлы будут в папке `build/`.

### Оптимизация сборки

- Минификация JavaScript и CSS
- Оптимизация изображений
- Code splitting
- Tree shaking

### Деплой

1. Соберите проект: `npm run build`
2. Загрузите содержимое папки `build/` на сервер
3. Настройте веб-сервер (nginx, Apache) для обслуживания статических файлов
4. Настройте редирект всех запросов на `index.html` для SPA

### Пример конфигурации nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/build;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /static {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 💻 Разработка

### Структура компонентов

Компоненты организованы по функциональности:

```
ComponentName/
├── ComponentName.jsx    # Основной компонент
├── ComponentName.scss   # Стили компонента
├── components/          # Подкомпоненты
│   └── SubComponent.jsx
└── hooks/               # Custom hooks
    └── useComponent.js
```

### Стилизация

Проект использует комбинацию подходов:

1. **SASS/SCSS** — для компонентных стилей
2. **Tailwind CSS** — для utility классов
3. **CSS Modules** — для изолированных стилей (в некоторых компонентах)
4. **Material-UI** — для готовых компонентов

### Custom Hooks

Создавайте переиспользуемые хуки в `src/hooks/`:

```javascript
// src/hooks/useCustomHook.js
import { useState, useEffect } from "react";

export const useCustomHook = (initialValue) => {
  const [value, setValue] = useState(initialValue);

  useEffect(() => {
    // Логика хука
  }, [value]);

  return [value, setValue];
};
```

### Работа с API

Все API запросы должны проходить через модули в `src/api/`:

```javascript
// src/api/example.js
import api from "./index";

export const getExampleData = () => {
  return api.get("/example/");
};

export const createExample = (data) => {
  return api.post("/example/", data);
};
```

### Работа с Redux

1. **Создание slice:**

```javascript
// src/store/slices/exampleSlice.js
import { createSlice } from "@reduxjs/toolkit";

const exampleSlice = createSlice({
  name: "example",
  initialState: { data: [] },
  reducers: {
    setData: (state, action) => {
      state.data = action.payload;
    },
  },
});

export const { setData } = exampleSlice.actions;
export default exampleSlice.reducer;
```

2. **Создание async thunk:**

```javascript
// src/store/creators/exampleCreators.js
import { createAsyncThunk } from "@reduxjs/toolkit";
import { getExampleData } from "../../api/example";

export const fetchExampleData = createAsyncThunk(
  "example/fetchData",
  async () => {
    const response = await getExampleData();
    return response.data;
  }
);
```

### Линтинг и форматирование

Проект использует ESLint конфигурацию Create React App:

```json
{
  "eslintConfig": {
    "extends": ["react-app", "react-app/jest"]
  }
}
```

---

## 🔒 Безопасность

### Аутентификация

- Использование JWT токенов (access + refresh)
- Токены хранятся в `localStorage`
- Автоматическое обновление токенов через interceptor

### Защита маршрутов

Все приватные маршруты защищены через `ProtectedRoute`:

- Проверка наличия компании
- Проверка срока подписки
- Автоматический редирект при отсутствии доступа

### API безопасность

- Все запросы включают Authorization header
- Автоматическая обработка 401 ошибок
- Очередь запросов при обновлении токена

### Рекомендации

1. **Не храните чувствительные данные в localStorage**
2. **Используйте HTTPS в production**
3. **Валидируйте данные на клиенте и сервере**
4. **Обрабатывайте ошибки безопасно**

---

## ⚡ Производительность

### Оптимизации

1. **Code Splitting** — автоматическое разделение кода через React.lazy
2. **Мемоизация** — использование useMemo и useCallback
3. **Виртуализация** — для больших списков (если используется)
4. **Debounce** — для поиска и фильтров
5. **Lazy Loading** — для изображений и компонентов

### Best Practices

1. **Избегайте лишних ре-рендеров:**

   - Используйте React.memo для компонентов
   - Мемоизируйте колбэки через useCallback
   - Мемоизируйте вычисления через useMemo

2. **Оптимизация запросов:**

   - Кэширование данных в Redux
   - Дебаунсинг поисковых запросов
   - Пагинация больших списков

3. **Оптимизация изображений:**
   - Использование WebP формата
   - Ленивая загрузка
   - Оптимизация размеров

### Мониторинг производительности

Используйте React DevTools Profiler для анализа производительности компонентов.

---

## 📚 Дополнительные ресурсы

### Документация

- [React Documentation](https://react.dev/)
- [Redux Toolkit Documentation](https://redux-toolkit.js.org/)
- [React Router Documentation](https://reactrouter.com/)
- [Material-UI Documentation](https://mui.com/)
- [i18next Documentation](https://www.i18next.com/)

### Внутренняя документация

- `ADAPTIVE_GUIDE.md` — руководство по адаптивности
- `ORDERS_API_DOCUMENTATION.md` — документация API заказов
- `CODE_ANALYSIS_SellMainStart.md` — анализ кода компонента продаж
- `TRANSFER_ACCEPTANCE_GUIDE.md` — руководство по переводам

---

## 🤝 Участие в разработке

### Процесс разработки

1. Создайте ветку от `master`
2. Внесите изменения
3. Протестируйте локально
4. Создайте Pull Request

### Соглашения о коде

- Используйте функциональные компоненты
- Используйте хуки вместо классов
- Следуйте структуре проекта
- Комментируйте сложную логику
- Используйте осмысленные имена переменных

### Сообщения коммитов

Используйте понятные сообщения коммитов:

```
feat: добавлена функция экспорта данных
fix: исправлена ошибка в расчете суммы
refactor: рефакторинг компонента продаж
docs: обновлена документация
```

---

## 📞 Контакты и поддержка

Для вопросов и поддержки обращайтесь к команде разработки.

---

## 📄 Лицензия

Проект является проприетарным. Все права защищены.

---

**Версия документации:** 1.0  
**Последнее обновление:** 2025
