# Каталог товаров - Production Catalog

## Описание

Компонент каталога товаров с возможностью перетаскивания карточек, детального просмотра и пролистывания фотографий.

## Функциональность

### ✅ Реализовано

- **Сетка товаров 3x3** с адаптивным дизайном
- **Перетаскивание карточек** (drag & drop) для изменения порядка
- **Детальный просмотр товара** в модальном окне
- **Пролистывание фотографий** в модальном окне
- **Поиск товаров** по названию
- **Фильтрация** по категориям, брендам, наличию
- **Переключение вида** (сетка/список)
- **Долгое нажатие** для активации перетаскивания
- **Адаптивный дизайн** для мобильных устройств

### 🔧 API интеграция

- **Redux store** для управления состоянием
- **API endpoints** для CRUD операций с товарами
- **Асинхронные действия** для загрузки данных
- **Обработка ошибок** и состояний загрузки

## Структура файлов

```
src/Components/Sectors/Production/Catalog/
├── ProductionCatalog.jsx          # Основной компонент
├── ProductionCatalog.scss         # Стили
└── README.md                      # Документация

src/api/
└── catalog.js                     # API для работы с каталогом

src/store/slices/
└── catalogSlice.js                # Redux slice для каталога
```

## Использование

### Базовое использование

```jsx
import ProductionCatalog from "./Components/Sectors/Production/Catalog/ProductionCatalog";

function App() {
  return <ProductionCatalog />;
}
```

### API методы

#### Получение товаров

```javascript
import { fetchProducts } from "../store/slices/catalogSlice";

// Загрузка всех товаров
dispatch(fetchProducts());

// Загрузка с фильтрами
dispatch(
  fetchProducts({
    category: "electronics",
    inStock: true,
    search: "phone",
  })
);
```

#### Создание товара

```javascript
import { createProduct } from "../store/slices/catalogSlice";

const newProduct = {
  name: "Новый товар",
  price: 15000,
  images: ["image1.jpg", "image2.jpg"],
  category: "electronics",
  brand: "Samsung",
  inStock: true,
};

dispatch(createProduct(newProduct));
```

#### Обновление порядка товаров

```javascript
import { updateProductsOrder } from "../store/slices/catalogSlice";

const productsOrder = [
  { id: 1, order: 0 },
  { id: 2, order: 1 },
  { id: 3, order: 2 },
];

dispatch(updateProductsOrder(productsOrder));
```

## Компоненты

### ProductCard

Карточка товара с возможностью перетаскивания и быстрыми действиями.

**Props:**

- `product` - объект товара
- `onView` - функция для открытия детального просмотра
- `onDragStart` - обработчик начала перетаскивания
- `onDragEnd` - обработчик окончания перетаскивания
- `isDragging` - флаг активного перетаскивания

### ProductDetailModal

Модальное окно для детального просмотра товара с галереей изображений.

**Props:**

- `product` - объект товара
- `isOpen` - флаг открытия модального окна
- `onClose` - функция закрытия модального окна

## Стили

### Основные классы

- `.production-catalog` - основной контейнер
- `.products-grid` - сетка товаров
- `.product-card` - карточка товара
- `.product-detail-modal` - модальное окно

### Состояния

- `.dragging` - активное перетаскивание
- `.long-press` - долгое нажатие
- `.loading` - состояние загрузки
- `.error` - состояние ошибки

## Адаптивность

### Breakpoints

- **Desktop**: 3 колонки
- **Tablet** (≤768px): 2 колонки
- **Mobile** (≤480px): 1 колонка

### Режимы просмотра

- **Grid** - сетка товаров
- **List** - список товаров

## API Endpoints

### Товары

- `GET /catalog/products` - получить все товары
- `GET /catalog/products/:id` - получить товар по ID
- `POST /catalog/products` - создать товар
- `PUT /catalog/products/:id` - обновить товар
- `DELETE /catalog/products/:id` - удалить товар

### Изображения

- `POST /catalog/products/:id/images` - загрузить изображение
- `DELETE /catalog/products/:id/images/:imageId` - удалить изображение

### Порядок

- `PUT /catalog/products/order` - обновить порядок товаров

### Поиск и фильтры

- `GET /catalog/products/search` - поиск товаров
- `GET /catalog/categories` - получить категории
- `GET /catalog/brands` - получить бренды

## Состояние Redux

```javascript
{
  catalog: {
    products: [],           // Массив товаров
    categories: [],        // Категории
    brands: [],           // Бренды
    selectedProduct: null, // Выбранный товар
    searchResults: [],    // Результаты поиска
    loading: false,       // Состояние загрузки
    error: null,          // Ошибка
    filters: {            // Фильтры
      category: null,
      brand: null,
      priceRange: null,
      inStock: null,
      sortBy: 'name',
      sortOrder: 'asc'
    },
    pagination: {         // Пагинация
      currentPage: 1,
      totalPages: 1,
      totalItems: 0,
      itemsPerPage: 9
    }
  }
}
```

## События

### Drag & Drop

- `onDragStart` - начало перетаскивания
- `onDragOver` - перетаскивание над элементом
- `onDrop` - сброс элемента
- `onDragEnd` - окончание перетаскивания

### Долгое нажатие

- `onMouseDown` - начало нажатия
- `onMouseUp` - окончание нажатия
- `onMouseLeave` - выход за пределы элемента

## Производительность

### Оптимизации

- **Lazy loading** изображений
- **Debounced search** для поиска
- **Memoized components** для предотвращения лишних рендеров
- **Virtual scrolling** для больших списков (планируется)

### Рекомендации

- Используйте изображения оптимального размера
- Реализуйте пагинацию для больших каталогов
- Кэшируйте часто используемые данные

## Планы развития

### Ближайшие улучшения

- [ ] Виртуальная прокрутка для больших списков
- [ ] Кэширование изображений
- [ ] Офлайн режим
- [ ] Экспорт/импорт товаров
- [ ] Массовые операции

### Дополнительные функции

- [ ] Сравнение товаров
- [ ] Избранные товары
- [ ] История просмотров
- [ ] Рекомендации
- [ ] Отзывы и рейтинги
