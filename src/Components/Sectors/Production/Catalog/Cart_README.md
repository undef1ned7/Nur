# Корзина - Cart Component

## Описание

Компонент корзины с выбором клиента, управлением товарами и сводкой заказа без доставки.

## Функциональность

### ✅ Реализовано

- **Список товаров в корзине** с изображениями и деталями
- **Выбор клиента** с поиском и выпадающим списком
- **Управление количеством** товаров (+/- кнопки)
- **Удаление товаров** из корзины
- **Сводка заказа** без доставки
- **Расчет общей суммы** с учетом скидок
- **Кнопка покупки** с проверкой выбора клиента
- **Адаптивный дизайн** для всех устройств

### 🔧 Redux интеграция

- **Управление состоянием** корзины через Redux
- **Синхронизация** с каталогом товаров
- **Персистентность** данных в localStorage
- **Селекторы** для оптимизации производительности

## Структура файлов

```
src/Components/Sectors/Production/Catalog/
├── Cart.jsx                    # Основной компонент корзины
├── Cart.scss                   # Стили корзины
└── Cart_README.md              # Документация

src/store/slices/
└── cartSlice.js                # Redux slice для корзины
```

## Использование

### Базовое использование

```jsx
import Cart from "./Components/Sectors/Production/Catalog/Cart";

function App() {
  return <Cart />;
}
```

### Добавление товара в корзину

```javascript
import { addToCart } from "../store/slices/cartSlice";

const product = {
  id: 1,
  name: "Товар",
  price: 15000,
  image: "image.jpg",
  // ... другие свойства
};

dispatch(
  addToCart({
    product,
    quantity: 1,
    store: "MyStore",
  })
);
```

### Обновление количества товара

```javascript
import { updateQuantity } from "../store/slices/cartSlice";

dispatch(
  updateQuantity({
    itemId: 123,
    quantity: 3,
  })
);
```

### Удаление товара из корзины

```javascript
import { removeFromCart } from "../store/slices/cartSlice";

dispatch(removeFromCart(itemId));
```

## Компоненты

### CartItem

Карточка товара в корзине с управлением количеством.

**Props:**

- `item` - объект товара в корзине
- `onUpdateQuantity` - функция обновления количества
- `onRemoveItem` - функция удаления товара

### ClientSelector

Выпадающий список для выбора клиента с поиском.

**Props:**

- `selectedClient` - выбранный клиент
- `onClientSelect` - функция выбора клиента
- `clients` - массив клиентов
- `loading` - состояние загрузки

### OrderSummary

Сводка заказа с расчетом суммы.

**Props:**

- `selectedClient` - выбранный клиент

## Redux Actions

### Основные действия

```javascript
// Добавить товар в корзину
addToCart({ product, quantity, store });

// Обновить количество товара
updateQuantity({ itemId, quantity });

// Удалить товар из корзины
removeFromCart(itemId);

// Очистить корзину
clearCart();

// Выбрать клиента
selectClient(client);

// Очистить выбор клиента
clearSelectedClient();
```

### Селекторы

```javascript
// Получить товары в корзине
const cartItems = useSelector(selectCartItems);

// Получить общую сумму
const total = useSelector(selectCartTotal);

// Получить количество товаров
const itemsCount = useSelector(selectCartItemsCount);

// Получить выбранного клиента
const selectedClient = useSelector(selectSelectedClient);
```

## Состояние Redux

```javascript
{
  cart: {
    items: [              // Товары в корзине
      {
        id: 123,
        product: { ... },  // Данные товара
        quantity: 2,       // Количество
        store: "Store",    // Магазин
        total: 30000       // Общая сумма
      }
    ],
    selectedClient: {     // Выбранный клиент
      id: 1,
      name: "Иван Иванов",
      phone: "+996 555 123 456"
    },
    loading: false,       // Состояние загрузки
    error: null          // Ошибка
  }
}
```

## Стили

### Основные классы

- `.cart-page` - основная страница корзины
- `.cart-container` - контейнер корзины
- `.cart-item` - карточка товара
- `.client-selector` - селектор клиента
- `.order-summary` - сводка заказа

### Состояния

- `.empty-cart` - пустая корзина
- `.loading` - состояние загрузки
- `.selected` - выбранный элемент

## Адаптивность

### Breakpoints

- **Desktop**: 2 колонки (товары + сводка)
- **Tablet** (≤1024px): 1 колонка
- **Mobile** (≤768px): адаптивные карточки

### Мобильная версия

- Вертикальное расположение элементов
- Увеличенные кнопки для touch
- Оптимизированные размеры

## Интеграция с каталогом

### Добавление товаров

```javascript
// В каталоге товаров
const handleAddToCart = (product) => {
  dispatch(
    addToCart({
      product,
      quantity: 1,
      store: "Default Store",
    })
  );
};
```

### Синхронизация данных

- Автоматическое обновление при изменении товаров
- Сохранение в localStorage
- Восстановление при перезагрузке

## API интеграция

### Клиенты

```javascript
// Загрузка клиентов
dispatch(fetchClientsAsync());

// Селектор клиентов
const { clients, loading } = useClient();
```

### Товары

```javascript
// Получение товаров из каталога
const products = useSelector(selectProducts);

// Добавление в корзину
dispatch(addToCart({ product, quantity, store }));
```

## Производительность

### Оптимизации

- **Мемоизация** компонентов
- **Селекторы** для предотвращения лишних рендеров
- **Ленивая загрузка** клиентов
- **Debounced search** для поиска

### Рекомендации

- Используйте React.memo для компонентов
- Оптимизируйте селекторы
- Кэшируйте данные клиентов
- Реализуйте виртуализацию для больших списков

## Планы развития

### Ближайшие улучшения

- [ ] Сохранение корзины в localStorage
- [ ] Восстановление корзины при перезагрузке
- [ ] Уведомления о добавлении товаров
- [ ] Быстрый заказ для постоянных клиентов

### Дополнительные функции

- [ ] Сравнение товаров
- [ ] Отложенные товары
- [ ] Рекомендации на основе корзины
- [ ] Промокоды и скидки
- [ ] История заказов
