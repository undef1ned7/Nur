# Анализ производительности отображения товаров - Warehouse

## 🔍 Текущее состояние производительности

### Метрики

- **Размер страницы**: 100 товаров
- **Debounce поиска**: 300ms ✅
- **Ленивая загрузка изображений**: ✅ (loading="lazy")
- **Мемоизация компонентов**: ✅ (React.memo)
- **Виртуализация**: ❌ Отсутствует

---

## ⚠️ Критические проблемы производительности

### 1. **Проблема: Пересоздание объекта params в useWarehouseData**

**Текущий код:**

```javascript
// Warehouse.jsx
const requestParams = useMemo(() => {
  const params = {
    page: currentPageFromUrl,
    ...filters,
  };
  if (debouncedSearchTerm?.trim()) {
    params.search = debouncedSearchTerm.trim();
  }
  return params;
}, [currentPageFromUrl, filters, debouncedSearchTerm]);

// useWarehouseData.js
useEffect(() => {
  if (params && Object.keys(params).length > 0) {
    dispatch(fetchProductsAsync(params));
  }
}, [dispatch, params]); // ⚠️ params - объект, пересоздается каждый раз
```

**Проблема:**

- Объект `params` пересоздается даже при неизменных значениях
- `useEffect` срабатывает при каждом изменении ссылки на объект
- Вызывает лишние запросы к API

**Влияние на производительность:**

- 🔴 **Критично**: Лишние HTTP запросы
- 🔴 **Критично**: Лишние ререндеры компонента

**Решение:**

```javascript
// Использовать сериализацию для сравнения
const paramsString = useMemo(
  () => JSON.stringify(requestParams),
  [requestParams]
);

useEffect(() => {
  if (params && Object.keys(params).length > 0) {
    dispatch(fetchProductsAsync(params));
  }
}, [dispatch, paramsString]); // Сравниваем строку, а не объект
```

---

### 2. **Проблема: Вычисления внутри map() при каждом рендере**

**Текущий код:**

```javascript
// ProductTable.jsx
{
  products.map((product, index) => {
    const primaryImage = getPrimaryImage(product); // ⚠️ Вычисляется каждый раз
    const isSelected = selectedRows.has(product.id); // ⚠️ Вычисляется каждый раз
    const rowNumber = getRowNumber(index, products.length); // ⚠️ Вычисляется каждый раз

    return <tr>...</tr>;
  });
}
```

**Проблема:**

- При каждом ререндере все вычисления выполняются заново
- Для 100 товаров = 300 вычислений на каждый ререндер
- `getPrimaryImage` ищет в массиве images при каждом рендере

**Влияние на производительность:**

- 🟡 **Средне**: Замедление рендеринга на 50-100ms
- 🟡 **Средне**: Увеличение времени отрисовки

**Решение:**

```javascript
// Мемоизировать вычисления для каждого товара
const productData = useMemo(() => {
  return products.map((product, index) => ({
    ...product,
    primaryImage: getPrimaryImage(product),
    isSelected: selectedRows.has(product.id),
    rowNumber: getRowNumber(index, products.length),
  }));
}, [products, selectedRows, getRowNumber]);
```

---

### 3. **Проблема: React.memo без кастомной функции сравнения**

**Текущий код:**

```javascript
// ProductTable.jsx
export default React.memo(ProductTable);
```

**Проблема:**

- `React.memo` использует поверхностное сравнение
- Функции (`onRowSelect`, `onProductClick`) пересоздаются при каждом рендере
- `selectedRows` (Set) всегда новая ссылка
- Компонент ререндерится даже при неизменных данных

**Влияние на производительность:**

- 🟡 **Средне**: Лишние ререндеры таблицы/карточек
- 🟡 **Средне**: Перерисовка всех строк при изменении одной

**Решение:**

```javascript
export default React.memo(ProductTable, (prevProps, nextProps) => {
  // Кастомное сравнение
  return (
    prevProps.products.length === nextProps.products.length &&
    prevProps.loading === nextProps.loading &&
    prevProps.isAllSelected === nextProps.isAllSelected &&
    prevProps.selectedRows.size === nextProps.selectedRows.size &&
    prevProps.products.every((p, i) => p.id === nextProps.products[i]?.id)
  );
});
```

---

### 4. **Проблема: Отсутствие мемоизации отдельных строк/карточек**

**Текущий код:**

```javascript
// ProductTable.jsx
{
  products.map((product, index) => <tr key={product.id}>...</tr>);
}
```

**Проблема:**

- Каждая строка/карточка ререндерится при любом изменении родителя
- Нет изоляции ререндеров на уровне отдельного товара

**Влияние на производительность:**

- 🟡 **Средне**: При изменении одного товара перерисовываются все
- 🟡 **Средне**: Медленная реакция на действия пользователя

**Решение:**

```javascript
// Создать мемоизированный компонент строки
const ProductRow = React.memo(({ product, isSelected, rowNumber, ... }) => {
  // ...
}, (prev, next) => {
  return (
    prev.product.id === next.product.id &&
    prev.isSelected === next.isSelected &&
    prev.rowNumber === next.rowNumber
  );
});
```

---

### 5. **Проблема: Синхронизация URL в usePagination**

**Текущий код:**

```javascript
// usePagination.js
useEffect(() => {
  const params = new URLSearchParams();
  if (currentPage > 1) {
    params.set("page", currentPage.toString());
  }
  const newSearchString = params.toString();
  const currentSearchString = searchParams.toString();
  if (newSearchString !== currentSearchString) {
    setSearchParams(params, { replace: true }); // ⚠️ Может вызывать ререндер
  }
}, [currentPage, searchParams, setSearchParams]);
```

**Проблема:**

- `setSearchParams` может вызывать ререндер всего компонента
- Проверка выполняется при каждом изменении `currentPage`
- Может создавать циклы обновлений

**Влияние на производительность:**

- 🟡 **Средне**: Лишние ререндеры при навигации
- 🟢 **Низко**: Незначительное влияние на UX

---

### 6. **Проблема: Отсутствие виртуализации для больших списков**

**Текущий код:**

```javascript
// ProductTable.jsx
{
  products.map((product, index) => <tr>...</tr>);
}
```

**Проблема:**

- Все 100 товаров рендерятся одновременно
- При большом количестве товаров (>200) производительность падает
- Нет отложенной загрузки невидимых элементов

**Влияние на производительность:**

- 🔴 **Критично**: При >200 товарах задержка 200-500ms
- 🔴 **Критично**: Проблемы на слабых устройствах

**Решение:**

- Использовать `react-window` или `react-virtualized`
- Рендерить только видимые строки (10-20)

---

### 7. **Проблема: Отсутствие кэширования справочников**

**Текущий код:**

```javascript
// useWarehouseReferences.js
useEffect(() => {
  dispatch(fetchBrandsAsync());
  dispatch(fetchCategoriesAsync());
}, [dispatch]); // ⚠️ Загружается при каждом монтировании
```

**Проблема:**

- Справочники загружаются каждый раз при открытии страницы
- Нет проверки, загружены ли уже данные

**Влияние на производительность:**

- 🟡 **Средне**: Лишние HTTP запросы
- 🟡 **Средне**: Задержка при первой загрузке

**Решение:**

```javascript
// Проверять наличие данных перед загрузкой
useEffect(() => {
  if (brands.length === 0) {
    dispatch(fetchBrandsAsync());
  }
  if (categories.length === 0) {
    dispatch(fetchCategoriesAsync());
  }
}, [dispatch, brands.length, categories.length]);
```

---

### 8. **Проблема: Отсутствие оптимистичных обновлений**

**Текущий код:**

```javascript
// Warehouse.jsx
const confirmBulkDelete = useCallback(async () => {
  // ...
  await dispatch(bulkDeleteProductsAsync({...})).unwrap();
  setSelectedRows(new Set());
  dispatch(fetchProductsAsync(requestParams)); // ⚠️ Ждем ответа сервера
}, [dispatch, selectedRows, requestParams]);
```

**Проблема:**

- UI обновляется только после ответа сервера
- Пользователь видит задержку при удалении

**Влияние на производительность:**

- 🟢 **Низко**: Влияет на воспринимаемую производительность
- 🟢 **Низко**: UX задержка 200-500ms

---

## 📊 Анализ времени рендеринга

### Текущие метрики (оценка)

| Операция                          | Время     | Критичность |
| --------------------------------- | --------- | ----------- |
| Первая загрузка (100 товаров)     | 150-300ms | 🟡 Средне   |
| Рендер таблицы (100 строк)        | 50-100ms  | 🟡 Средне   |
| Рендер карточек (100 карточек)    | 80-150ms  | 🟡 Средне   |
| Обновление при изменении фильтров | 200-400ms | 🟡 Средне   |
| Навигация по страницам            | 150-300ms | 🟡 Средне   |
| Массовое удаление                 | 300-600ms | 🟡 Средне   |

### Потенциальные улучшения

| Оптимизация               | Ожидаемое улучшение         | Сложность  |
| ------------------------- | --------------------------- | ---------- |
| Мемоизация вычислений     | -30-50ms                    | 🟢 Низкая  |
| Виртуализация списков     | -100-200ms                  | 🟡 Средняя |
| Кэширование справочников  | -50-100ms                   | 🟢 Низкая  |
| Оптимизация React.memo    | -20-40ms                    | 🟢 Низкая  |
| Мемоизация строк/карточек | -30-60ms                    | 🟡 Средняя |
| Оптимистичные обновления  | -200-400ms (воспринимаемое) | 🟡 Средняя |

---

## 🎯 Приоритетные оптимизации

### Критичные (высокий приоритет)

1. **Исправить пересоздание params объекта**

   - Влияние: Устранение лишних HTTP запросов
   - Сложность: 🟢 Низкая
   - Время: 30 минут

2. **Добавить виртуализацию для больших списков**
   - Влияние: Улучшение производительности на 50-70%
   - Сложность: 🟡 Средняя
   - Время: 2-3 часа

### Важные (средний приоритет)

3. **Мемоизировать вычисления в map()**

   - Влияние: Ускорение рендеринга на 30-50%
   - Сложность: 🟢 Низкая
   - Время: 1 час

4. **Оптимизировать React.memo с кастомным сравнением**

   - Влияние: Уменьшение ререндеров на 40-60%
   - Сложность: 🟢 Низкая
   - Время: 1 час

5. **Кэшировать справочники**
   - Влияние: Устранение лишних запросов
   - Сложность: 🟢 Низкая
   - Время: 30 минут

### Желательные (низкий приоритет)

6. **Мемоизировать отдельные строки/карточки**

   - Влияние: Улучшение отзывчивости
   - Сложность: 🟡 Средняя
   - Время: 2 часа

7. **Добавить оптимистичные обновления**
   - Влияние: Улучшение UX
   - Сложность: 🟡 Средняя
   - Время: 1-2 часа

---

## 🔧 Конкретные рекомендации по оптимизации

### 1. Исправление useWarehouseData

```javascript
// hooks/useWarehouseData.js
export const useWarehouseData = (params) => {
  const dispatch = useDispatch();
  const { list: products, loading, count, next, previous } = useProducts();

  // Сериализация params для стабильного сравнения
  const paramsString = useMemo(() => JSON.stringify(params || {}), [params]);

  // Загрузка товаров при изменении параметров
  useEffect(() => {
    if (params && Object.keys(params).length > 0) {
      dispatch(fetchProductsAsync(params));
    }
  }, [dispatch, paramsString]); // Используем строку вместо объекта

  const filteredProducts = useMemo(() => products, [products]);

  return {
    products: filteredProducts,
    loading,
    count,
    next,
    previous,
  };
};
```

### 2. Мемоизация вычислений товаров

```javascript
// components/ProductTable.jsx
const ProductTable = ({ products, selectedRows, getRowNumber, ... }) => {
  // Мемоизация данных товаров
  const productsData = useMemo(() => {
    return products.map((product, index) => ({
      ...product,
      primaryImage: getPrimaryImage(product),
      isSelected: selectedRows.has(product.id),
      rowNumber: getRowNumber(index, products.length),
    }));
  }, [products, selectedRows, getRowNumber]);

  return (
    <tbody>
      {productsData.map((productData) => (
        <ProductRow key={productData.id} productData={productData} />
      ))}
    </tbody>
  );
};
```

### 3. Мемоизированный компонент строки

```javascript
// components/ProductRow.jsx
const ProductRow = React.memo(
  ({ productData, onRowSelect, onProductClick }) => {
    return (
      <tr
        className="warehouse-table__row"
        onClick={() => onProductClick(productData)}
      >
        {/* ... */}
      </tr>
    );
  },
  (prev, next) => {
    return (
      prev.productData.id === next.productData.id &&
      prev.productData.isSelected === next.productData.isSelected &&
      prev.productData.primaryImage?.image_url ===
        next.productData.primaryImage?.image_url
    );
  }
);
```

### 4. Виртуализация таблицы

```javascript
// Использование react-window
import { FixedSizeList } from 'react-window';

const VirtualizedProductTable = ({ products, ... }) => {
  const Row = ({ index, style }) => {
    const product = products[index];
    // ...
    return <tr style={style}>...</tr>;
  };

  return (
    <FixedSizeList
      height={600}
      itemCount={products.length}
      itemSize={60}
    >
      {Row}
    </FixedSizeList>
  );
};
```

### 5. Кэширование справочников

```javascript
// hooks/useWarehouseReferences.js
export const useWarehouseReferences = () => {
  const dispatch = useDispatch();

  const brands = useSelector((state) => state.product.brands || []);
  const categories = useSelector((state) => state.product.categories || []);

  useEffect(() => {
    // Загружаем только если данных нет
    if (brands.length === 0) {
      dispatch(fetchBrandsAsync());
    }
    if (categories.length === 0) {
      dispatch(fetchCategoriesAsync());
    }
  }, [dispatch, brands.length, categories.length]);

  return { brands, categories };
};
```

---

## 📈 Ожидаемые результаты после оптимизаций

### До оптимизаций:

- Первая загрузка: **200-400ms**
- Рендер 100 товаров: **80-150ms**
- Навигация: **200-300ms**
- Обновление фильтров: **300-500ms**

### После оптимизаций:

- Первая загрузка: **100-200ms** (-50%)
- Рендер 100 товаров: **30-60ms** (-60%)
- Навигация: **100-150ms** (-50%)
- Обновление фильтров: **150-250ms** (-50%)

### При использовании виртуализации:

- Рендер 1000 товаров: **50-100ms** (вместо 500-1000ms)
- Память: **-70%** (рендерятся только видимые элементы)

---

## 🧪 Рекомендации по тестированию производительности

### Инструменты:

1. **React DevTools Profiler** - для измерения времени рендеринга
2. **Chrome Performance** - для анализа производительности
3. **Lighthouse** - для общей оценки производительности

### Метрики для отслеживания:

- Time to First Render (TTFR)
- Time to Interactive (TTI)
- Количество ререндеров компонентов
- Время выполнения запросов API
- Использование памяти

---

## 📝 Чеклист оптимизаций

### Критичные (сделать немедленно):

- [ ] Исправить пересоздание params объекта
- [ ] Добавить виртуализацию для списков >50 товаров
- [ ] Мемоизировать вычисления в map()

### Важные (сделать в ближайшее время):

- [ ] Оптимизировать React.memo с кастомным сравнением
- [ ] Кэшировать справочники (бренды, категории)
- [ ] Мемоизировать отдельные строки/карточки

### Желательные (сделать при возможности):

- [ ] Добавить оптимистичные обновления
- [ ] Добавить skeleton loading для изображений
- [ ] Оптимизировать размер изображений (WebP, сжатие)
- [ ] Добавить prefetch для следующей страницы

---

## 🎯 Итоговая оценка производительности

### Текущее состояние: **6/10**

**Сильные стороны:**

- ✅ Debounce для поиска
- ✅ Ленивая загрузка изображений
- ✅ Пагинация
- ✅ React.memo на компонентах

**Слабые стороны:**

- ❌ Отсутствие виртуализации
- ❌ Лишние HTTP запросы
- ❌ Неоптимальные ререндеры
- ❌ Отсутствие кэширования

### После оптимизаций: **9/10**

**Ожидаемые улучшения:**

- ⚡ Ускорение рендеринга на 50-70%
- ⚡ Уменьшение количества запросов на 30-50%
- ⚡ Улучшение отзывчивости UI
- ⚡ Поддержка больших списков (>1000 товаров)
